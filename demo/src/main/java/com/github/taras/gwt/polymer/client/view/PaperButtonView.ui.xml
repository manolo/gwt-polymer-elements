<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:g='urn:import:com.google.gwt.user.client.ui'>
    
    <ui:style>
        section {
            padding: 20px 0;
        }

        section > div {
            padding: 14px;
            font-size: 16px;
        }

        paper-button.colored {
            color: #4285f4;
        }

        paper-button[raised].colored {
            background: #4285f4;
            color: #fff;
        }

        paper-button.custom > core-icon {
            margin-right: 4px;
        }

        paper-button.hover:hover {
            background: #eee;
        }

        paper-button.blue-ripple::shadow #ripple {
            color: #4285f4;
        }
    </ui:style>
    
    <g:HTMLPanel>
        <section>

            <div>Flat buttons</div>

            <paper-button>button</paper-button>
            <paper-button ui:field="flatColoredButton" class="{style.colored}">colored</paper-button>
            <paper-button disabled="">disabled</paper-button>
            <paper-button noink="">noink</paper-button>

        </section>

        <br />

        <section>

            <div>Raised buttons</div>

            <paper-button raised="">button</paper-button>
            <paper-button raised="" class="colored">colored</paper-button>
            <paper-button raised="" disabled="">disabled</paper-button>
            <paper-button raised="" noink="">noink</paper-button>

        </section>

        <section>

            <div>Custom button content</div>

            <paper-button class="{style.colored} {style.custom}">
                <core-icon icon="check"></core-icon>
                ok
            </paper-button>
            <paper-button class="{style.custom}">
                <core-icon icon="clear"></core-icon>
                cancel
            </paper-button>
            <br />
            <paper-button>
                <a href="https://www.polymer-project.org" target="_blank">link</a>
            </paper-button>

        </section>

        <section>

            <div>Toggle buttons</div>

            <paper-button ui:field="toggleButton" toggle="">button</paper-button>

            <paper-button toggle="" raised="" noink="">noink</paper-button>

            <paper-button toggle="" active="" class="{style.colored}">active</paper-button>

            <paper-button toggle="" raised="" active="" class="{style.colored}">active</paper-button>

        </section>

        <section>

            <div>Styling options</div>

            <paper-button class="{style.hover}">hover</paper-button>
            <paper-button class="{style.blue-ripple}">custom ripple</paper-button>

        </section>
    </g:HTMLPanel>
</ui:UiBinder>