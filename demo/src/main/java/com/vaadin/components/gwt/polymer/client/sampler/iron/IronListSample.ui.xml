<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:g='urn:import:com.google.gwt.user.client.ui'
             xmlns:i='urn:import:com.vaadin.polymer.iron.widget'
             xmlns:p='urn:import:com.vaadin.polymer.paper.widget'>

    <ui:style>

    </ui:style>

    <g:HTMLPanel>
        <style is="custom-style">
            paper-scroll-header-panel {
                @apply(--layout-fit);
                @apply(--layout-vertical);
                @apply(--paper-font-common-base);
            }

            paper-toolbar.tall .title {
                font-size: 40px;
                margin-left: 60px;

                -webkit-transform-origin: left center;
                transform-origin: left center;
                overflow: visible;
            }

            iron-list {
                background-color: var(--paper-grey-200, #eee);
                padding-bottom: 16px;
            }

            .item {
                @apply(--layout-horizontal);

                margin: 16px 16px 0 16px;
                padding: 20px;

                border-radius: 8px;
                background-color: white;
                border: 1px solid #ddd;
            }

            .avatar {
                height: 40px;
                width: 40px;
                border-radius: 20px;
                box-sizing: border-box;
                background-color: #DDD;
            }

            .pad {
                padding: 0 16px;
                @apply(--layout-flex);
                @apply(--layout-vertical);
            }

            .primary {
                font-size: 16px;
                font-weight: bold;
            }

            .secondary {
                font-size: 14px;
            }

            .dim {
                color: gray;
            }

        </style>

        <!--<template is="dom-bind">-->
            <i:IronAjax ui:field="ajax" url="gwtPolymerDemo/contacts.json" auto="true"/>

            <p:PaperScrollHeaderPanel addStyleNames="fit" condenses="false" keepCondensedHeader="false">
                <p:PaperToolbar>
                    <p:PaperIconButton icon="arrow-back"/>
                    <div class="flex"/>
                    <p:PaperIconButton icon="search"/>
                    <p:PaperIconButton icon="more-vert"/>
                    <div class="bottom title">iron-list</div>
                </p:PaperToolbar>
                <i:IronList ui:field="list" as="item">
                    <template>
                        <div>
                            <div class="item">
                                <img class="avatar" src="[[item.image]]"/>
                                <div class="pad">
                                    <div class="primary">[[item.name]]</div>
                                    <div class="secondary">[[item.shortText]]</div>
                                    <div class="secondary dim">[[item.longText]]</div>
                                </div>
                                <!--<i:IronIcon icon="star"/>-->
                            </div>
                        </div>
                    </template>
                </i:IronList>
            </p:PaperScrollHeaderPanel>
        <!--</template>-->

    </g:HTMLPanel>
</ui:UiBinder>