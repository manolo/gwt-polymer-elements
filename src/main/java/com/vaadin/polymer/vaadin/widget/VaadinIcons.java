/*
 * This code was generated with Vaadin Web Component GWT API Generator, 
 * from vaadin-icons project by Vaadin Ltd
 * that is licensed with Apache-2.0 license.
 */
package com.vaadin.polymer.vaadin.widget;

import com.vaadin.polymer.vaadin.*;

import com.vaadin.polymer.*;
import com.vaadin.polymer.elemental.*;
import com.vaadin.polymer.PolymerWidget;
import com.google.gwt.core.client.JsArray;
import com.google.gwt.event.shared.HandlerRegistration;
import com.google.gwt.core.client.JavaScriptObject;

/**
 * <p> <code>vaadin-icons</code> is a set of 600+ icons which can be used together with Polymer’s <a href="https://elements.polymer-project.org/elements/iron-icon"><code>iron-icon</code></a> component.</p>
 * <p> To use the <code>vaadin-icons</code> iconset, import the specific <code>vaadin-icons.html</code>, and<br> specify the icon as <code>vaadin:&lt;icon&gt;</code>. For example, to use a cart icon, you would<br> use:</p>
 * <pre><code class="lang-html">&lt;link rel=&quot;import&quot; href=&quot;/components/vaadin-icons/vaadin-icons.html&quot;&gt;
 * &lt;iron-icon icon=&quot;vaadin:cart&quot;&gt;&lt;/iron-icon&gt;
 * </code></pre>
 * <p> For the complete list of available icons, see <a href="https://vaadin.com/icons">https://vaadin.com/icons</a></p>
 */
public class VaadinIcons extends PolymerWidget {
    /**
     * Default Constructor.
     */
    public VaadinIcons() {
       this("");
    }

    /**
     * Constructor used by UIBinder to create widgets with content.
     */
    public VaadinIcons(String html) {
        super(VaadinIconsElement.TAG, VaadinIconsElement.SRC, html);
    }

    /**
     * Gets a handle to the Polymer object's underlying DOM element.
     */
    public VaadinIconsElement getPolymerElement() {
        return (VaadinIconsElement) getElement();
    }





}
